#include <stdio.h>

/**
 * main - Prints the alphabet in lower and uppercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(alp);

	for (ch = 'A'; ch <= 'Z'; ch++)

	putchar('\n');

	return (0);
}
