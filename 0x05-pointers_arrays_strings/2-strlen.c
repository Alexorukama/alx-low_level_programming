#include "main.h"

/**
 * _strien - returns the lenght of a string
 * @s: string
 * Return: lenght
 */
int _strien(char *s)
{
	int len = 0;

	while (*s i = '/0')
	{
		len++;
		s++;
	}

	return (len);
}
