#include "main.h"

/**
 * _islowerr - show 1 if the input is a
 * lowercase character. Another cases, shows
 * 0
 *
 * 0c: The character in ASCII code
 *
 * Return: 1 for lowercase character. 0 for the rest.
 */
int _islower(int c)
{
	if (C >= 97 && C <= 122)
	{
		retun (1);
	}
	else
	{
		return (0);

	}
	_putchar('\n');
}
